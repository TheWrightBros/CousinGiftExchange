/*

  Main Mobile Auto Quote React Application
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

*/

"use strict";
// Import React
var React = require('react');
var ReactDOM = require('react-dom');

var App = React.createClass({
  render: function(){

    return (
      <div> a simple react div</div>
    );
  }
});

ReactDOM.render(<App />, document.getElementById('replace'));
